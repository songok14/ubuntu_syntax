# 현재 위치경로 출력
pwd

# 현재위치에서 파일 및 폴더 조회
ls

# 목록조회 자세히
ls -l

# 목록조회 자세히 + 숨김파일
ls -al

# 목록조회 자세히 + 숨김파일 + 오래된순 정렬(최신은 alt)
ls -alrt

# 디렉토리 생성
mkdir songok_dir

# 특정 디렉토리로 이동
cd songok_dir

# 상위 디렉토리로 이동
cd ..  # 또는 cd ../

# 루트(최상위) 디렉토리로 이동
cd /

# 직전 디렉토리로 이동
cd -

# 홈경로(/home/본인계정)로 이동
cd

# 상대경로와 절대경로
# 상대경로: cd ../../
# 절대경로: cd /home
